require "lib.utils"

local check = function(args, players, log)
	os.exit(0)
	-- luacheck
	-- check documentation is covering
	-- check archive has files with proper names for all weeks
end

return check
